# As-built -- factual, living, never frozen

What is **deployed**, described so another developer can code against it.
One file per component.

This tree is written in exactly one place in the flow: at release. It is
never written forward-looking -- an as-built containing intentions is
worthless. If it is not shipped, it does not belong here.

Never frozen: it is corrected whenever reality moves.
